overwrite: true
schema: "./src/modules/**/*.graphql"
documents: ""
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
  ./graphql.schema.json:
    plugins:
      - "introspection"
